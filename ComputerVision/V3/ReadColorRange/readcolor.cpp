#include "readcolor.h"
#include <fstream>
#include <iostream>
#include <opencv2/core/types.hpp>
#include <string>
#include <vector>

std::vector<std::vector<cv::Scalar>> readcolor(std::string path) {
  std::vector<std::vector<cv::Scalar>> colors;
  return colors;
}
